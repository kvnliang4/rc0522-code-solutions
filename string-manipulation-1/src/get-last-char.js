/* exported getLastChar */

/* use string.length to find the length of the string */
/* find last index of string by doing string.length - 1 */
/* return string[string.length - 1]; */

function getLastChar(string) {
  return string[string.length - 1];
}
