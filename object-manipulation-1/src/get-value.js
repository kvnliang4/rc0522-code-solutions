/* exported getValue */

/* return object[key] to get the value of the property */

function getValue(object, key) {
  var value = object[key];
  return value;
}

/* value of object is object[key] */
/* assign object[key] to new variable value */
/* return value */
