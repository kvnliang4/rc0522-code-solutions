/* exported last */
/* last element of array  = length of array - 1 */
/* can use array.length to get length of array and subtract 1 to get index of last element */
/* return array[array.length - 1]; */

function last(array) {
  return array[array.length - 1];
}
